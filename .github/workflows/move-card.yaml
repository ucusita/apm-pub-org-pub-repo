name: Label moved card
on:
  issues:
    types:
      - assigned
jobs:
  automate-project-columns:
    runs-on: ubuntu-latest
    steps:
      - uses: alex-page/github-project-automation-plus@v0.3.0
        with:
          project: apm-org-board
          column: c2
          repo-token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
